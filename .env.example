# Discord Bot Token（Developer Portal から取得）
DISCORD_BOT_TOKEN=your-bot-token-here

# チャンネル登録は MCP ツール register_channel で行う（.envへの設定不要）
# claude mcp add agent-log で接続後:
#   register_channel(channel_id=1234..., webhook_url="https://...", name="project-a")
